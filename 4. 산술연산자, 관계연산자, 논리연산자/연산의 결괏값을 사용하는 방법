#include <stdio.h>

int main(void)
{
    int a = 10. b = 20. res;
    
    a + b; // 연산 결과는 버려짐
    printf("%d + %d = %d\n", a, b, a+b);
    
    res = a + b;
    printf(%d + %d = %d\n", a, b, res); // 연산 결과를 바로 출력에 사용
   
    return 0;
}
